{% trans_default_domain 'TecnocreacionesVzlaGovernmentBundle' %}

{% macro create(url) %}
<div class="field-block button-height">
    <button type="submit" class="button glossy mid-margin-right">
        <span class="button-icon"><span class="icon-tick"></span></span>
        {{ 'tecnocreaciones_vzla_government.register'|trans }}
    </button>
    <a href="{{ app.request.headers.get('referer', url) }}" class="button glossy">
        <span class="button-icon red-gradient"><span class="icon-cross-round"></span></span>
        {{ 'tecnocreaciones_vzla_government.cancel'|trans }}
    </a>
</div>
{% endmacro %}

{% macro update(url) %}
<div class="field-block button-height">
    <button type="submit" class="button glossy mid-margin-right">
        <span class="button-icon"><span class="icon-tick"></span></span>
        {{ 'tecnocreaciones_vzla_government.save'|trans }}
    </button>
    <a href="{{ app.request.headers.get('referer', url) }}" class="button glossy">
        <span class="button-icon red-gradient"><span class="icon-cross-round"></span></span>
        {{ 'tecnocreaciones_vzla_government.cancel'|trans }}
    </a>
</div>
{% endmacro %}
